version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - npm ci
  pre_build:
    commands:
      - echo "Installing dependencies complete"
  build:
    commands:
      - npm run build
artifacts:
  base-directory: dist/
  files:
    - '**/*'